# Build artifacts and local installs — npm ci installs fresh inside the image.
node_modules
dist
.angular

# Not needed in the build context
.git
.vscode
.idea
pom.xml
target
*.md
Dockerfile
.dockerignore

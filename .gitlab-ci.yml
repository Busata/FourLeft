stages:
  - deploy

deploy_job:
  stage: deploy
  before_script:
    - mkdir -p ~/.ssh
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' > ~/.ssh/id_rsa
    - chmod 700 ~/.ssh/id_rsa
    - eval "$(ssh-agent -s)"
    - ssh-add ~/.ssh/id_rsa
  script:
    - ssh -vo StrictHostKeyChecking=no -p 19870 busata@178.79.180.237 "./deploy-fourleft-server.sh && exit"

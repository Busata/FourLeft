package io.busata.fourleft.backendacrally.domain.models.championship;

/** Whether a championship is still being built ({@code DRAFT}) or visible to members ({@code PUBLISHED}). */
public enum ChampionshipStatus {
    DRAFT,
    PUBLISHED
}
